# 环境搭建与运行时配置（Phase 0 记录）

> 记录 AI 教师系统开发环境的搭建过程与关键配置，便于重建环境或排查问题。
> 最后更新：2026-08-14

## 1. 运行时架构

```
前端 (UniApp H5) ──> 后端 (03-Backend) ──> AgentSkills Runtime (仓颉二进制)
                                              ├── PostgreSQL (uctoo 数据库, RBAC 权限)
                                              └── LLM: Agnes (OpenAI 兼容接口)
```

- **运行时**：`@opencangjie/skills`（SDK 1.0.2），内置 Runtime v0.0.22
- **数据库**：PostgreSQL 17（本地，服务名 `postgresql-x64-17`）
- **LLM**：Agnes `agnes-2.0-flash`（OpenAI 兼容，Base URL `https://apihub.agnes-ai.com/v1`）

## 2. 关键路径

| 用途 | 路径 |
|------|------|
| SDK & Runtime | `03-Backend\node_modules\@opencangjie\skills` |
| 运行时二进制 | `...\dist\runtime\win-x64\release\bin\agentskills-runtime.exe` |
| 运行时配置 | `...\dist\runtime\win-x64\release\bin\.env`（**必须放 bin/ 目录**） |
| 运行时日志 | `...\dist\runtime\win-x64\release\bin\logs\agentskills-runtime.log` |
| 运行时源码参考 | `06-Tools\agentskills-runtime-src`（atomgit UCToo/agentskills-runtime，main 分支，含 SQL） |
| 数据库 SQL | `06-Tools\agentskills-runtime-src\sql\`（uctooDB.sql 建表、uctoov4InitData.sql 初始数据） |

## 3. 数据库初始化（PostgreSQL 17）

```powershell
# 1. 设置 postgres 密码（与运行时默认连接串一致）
$env:PGPASSWORD = "postgres"
psql -U postgres -h 127.0.0.1 -p 5432 -c "ALTER USER postgres WITH PASSWORD 'uctoo123';"

# 2. 创建数据库
psql -U postgres -h 127.0.0.1 -p 5432 -c "CREATE DATABASE uctoo;"

# 3. 导入建表脚本
$env:PGPASSWORD = "uctoo123"
psql -U postgres -h 127.0.0.1 -p 5432 -d uctoo -v ON_ERROR_STOP=1 -f uctooDB.sql
```

### 初始数据导入（重要）

`uctoov4InitData.sql` 有两个问题需绕过：

1. **permissions 表外键顺序**：INSERT 依赖父记录，需禁用外键检查：
   - 用 Python 在文件头部注入 `SET session_replication_role = replica;`（勿用 PowerShell 改写，会破坏 UTF-8 编码）
   - 修改版已存为 `06-Tools\uctoov4InitData_nofk.sql`

2. **lang 表结构不匹配**：脚本含 `INSERT INTO lang (..., permission_name, ...)`，但 uctooDB.sql 的 lang 表无此列，导入会中断 —— **lang 数据可跳过**，不影响运行时（关键数据在中断前已导入）。

3. **user_has_roles 脏数据**：脚本内引用不存在的 role_id（863e8a2a...），需手动给 demo 用户关联超级管理员角色：

```powershell
psql -U postgres -h 127.0.0.1 -p 5432 -d uctoo -c "INSERT INTO user_has_roles (id, user_id, role_id, status, creator, created_at, updated_at) VALUES (gen_random_uuid(), '505cf909-5e0e-4dde-b215-74274d2cc548', '2338cf7b-82b7-4cf6-b1ae-6a3ca79d683d', 1, '505cf909-5e0e-4dde-b215-74274d2cc548', now(), now());"
```

### 认证协议（坑）

OpenGauss 驱动只支持 **md5** 认证，PostgreSQL 17 默认 scram-sha-256，必须：

```powershell
# 1. 修改 C:\Program Files\PostgreSQL\17\data\pg_hba.conf
#    host all all 127.0.0.1/32 scram-sha-256  →  md5
#    host all all ::1/128     scram-sha-256  →  md5
# 2. 将密码哈希转为 md5 格式
psql -U postgres -h 127.0.0.1 -p 5432 -c "SET password_encryption = 'md5'; ALTER USER postgres WITH PASSWORD 'uctoo123';"
```

不修改会报：`SqlException: invalid AuthenticationRFC5802 message`。

## 4. 运行时 .env 配置

文件位置：`03-Backend\node_modules\@opencangjie\skills\dist\runtime\win-x64\release\bin\.env`

```ini
# LLM 配置（Agnes，OpenAI 兼容）
MODEL_CONFIG=openai:agnes-2.0-flash
OPENAI_API_KEY=sk-xxxxxxxx   # Agnes 平台生成
OPENAI_BASE_URL=https://apihub.agnes-ai.com/v1

# 数据库（默认值即此，无需显式设置亦可）
# orm_drivers=opengauss
# orm_defaultDriver=opengauss
# orm_connectionUrl=postgresql://postgres:uctoo123@127.0.0.1:5432/uctoo
```

注意：
- `.env` 必须与 exe 同目录（bin/），不是 release/ 目录
- 也可用 `MODEL_PROVIDER=openai` + `MODEL_NAME=agnes-2.0-flash` 代替 MODEL_CONFIG
- JWT 密钥：源码默认 `uctoo-v4-secret-key`（可用 `JWT_SECRET` 覆盖）

## 5. JWT 认证（开发用）

所有 `/api/v1/uctoo/*` 接口需 Bearer Token（RBAC）。开发验证可用 Node 自签：

```javascript
// 用 HMAC-SHA256 签 JWT，payload: { user: "<userId>", session: "<sessionId>", iat, exp }
// 密钥: uctoo-v4-secret-key（或 JWT_SECRET）
// 用户: demo 用户 id = 505cf909-5e0e-4dde-b215-74274d2cc548
```

## 6. 启动与验证

```powershell
# 启动（后台）
npx skills start
# 或直接运行二进制（前台，便于看日志）
& "...\release\bin\agentskills-runtime.exe"

# 验证
curl http://127.0.0.1:8080/hello
curl http://127.0.0.1:8080/api/v1/ai/health                     # LLM 服务
curl -H "Authorization: Bearer <jwt>" http://127.0.0.1:8080/api/v1/uctoo/skills

# 安装技能（API）
POST /api/v1/uctoo/skills/add   body: {"source": "<本地路径或 git 地址>"}
# 执行技能
POST /api/v1/uctoo/skills/execute  body: {"skill_id": "<id>", "input": "..."}
```

## 7. 已知问题

- 运行时日志会输出大量 `===` 调试信息（stderr），属正常现象
- 数据库 ORM 初始化失败日志：v0.0.22 尝试连 `nuanshou` 库失败（`Invalid unicode scalar value`）—— 仅影响 ORM 的 CRUD 部分，Skill/RBAC 认证走 DatabaseConnectionPool 正常。若需消除，可在 .env 显式设置 `orm_connectionUrl`
- Skill 安装位置：`...\release\bin\skills\`（相对二进制目录），GitManager 会 git clone 到 `.\temp\git-clone`

## 8. Windows DLL 依赖（坑）

运行时为 MinGW/仓颉编译，缺 GCC 运行时 DLL 会以退出码 `-1073741515 (0xC0000135)` 崩溃。已补入 `release\bin\`：
`libssp-0.dll`、`libwinpthread-1.dll`、`libgcc_s_seh-1.dll`、`libstdc++-6.dll`

若重装运行时后崩溃，需重新补齐这些 DLL（来源：MSYS2 仓库 + JuliaBinaryWrappers/CompilerSupportLibraries_jll）。